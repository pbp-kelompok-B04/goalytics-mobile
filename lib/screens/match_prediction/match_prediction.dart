import 'package:flutter/material.dart';

class MatchPredictionPage extends StatelessWidget {
  const MatchPredictionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Match Prediction")),
      body: const Center(child: Text("Match Prediction Page")),
    );
  }
}
